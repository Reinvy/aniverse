## Summary

E2E Navigation & UI Integration Testing cycle (C4) — 2026-08-11.

### What was changed
- **`e2e/api.spec.ts`** (+6 tests, 87 lines):
  - `GET /api/marketplace` — 200 + shape contract (products array, pagination meta, stats bar with `totalListings` / `activeCreators` / `avgPrice`)
  - `GET /api/marketplace?sort=price-asc` — 200 + ascending price order assertion
  - `GET /api/marketplace` cursor pagination — page1 emits `nextCursor`, page2 has zero overlap (keyset contract)
  - `GET /api/marketplace?search=` — graceful 200 with array on non-matching query (never 500)
  - `GET /api/artworks/[id]` — 405 Method Not Allowed (route mounted, PATCH/DELETE only)
  - `PATCH /api/artworks/[id]` unauthenticated — 401/403 (ownership-scoped guard)
- **`e2e/dashboard.spec.ts`** (+2 tests, 95 lines):
  - **Full auth-flow integration test**: register (or login on 409) → seed cookie + localStorage (mirrors `setStoredToken` two-layer session) → `/dashboard` loads → sidebar `:visible` nav links navigate through all 6 sub-pages (gallery/create/marketplace/social/challenges/monetization) without redirecting back to login
  - **Authenticated `/api/dashboard/stats`** — 200 with nested `{ user, stats, activity }` shape assertions

### Why
- `/api/marketplace` and `/api/artworks/[id]` were the only untested API routes; the authenticated dashboard path had zero end-to-end coverage (register → login → dashboard navigation is the app's core user journey).
- The auth-flow test documents the two-layer session requirement (server-side proxy cookie + client-side AuthProvider localStorage) as a permanent regression guard.

### How to test
```bash
cd /opt/data/workspace/aniverse
npx playwright test --reporter=list   # 75 tests total
npm run build                          # clean build
```

### Fix log (self-healing loop)
1. ❌ `GET /api/dashboard/stats` shape assertion failed → stats are nested under `body.stats` (not top-level) → fixed assertion
2. ❌ Auth-flow test landed on `/login` (AuthProvider reads localStorage, cookie alone insufficient) → added `addInitScript` localStorage seeding
3. ❌ Proxy still redirected on first request (initScript cookie set too late) → added context-level `addCookies` for the proxy + `addInitScript` for the client
4. ❌ Sidebar nav: `.first()` matched the off-screen mobile `<aside>` → switched to `:visible` selectors
5. ✅ Final: **75/75 tests PASS** + clean `npm run build`

### Environment variables affected
- None.

Closes: E2E coverage gaps for marketplace/artworks APIs and authenticated dashboard navigation.
